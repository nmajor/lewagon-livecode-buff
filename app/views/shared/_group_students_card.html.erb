<h3><%= group.name %></h3>
<!-- <%= link_to "Submit Code" %> -->
<ul class="division-group-card list-group">
  <% group.students.each do |student| %>
    <li class="list-group-item student-card">
      <%= image_tag student.avatar_url, class: "avatar" %>
      <div class="name"><%= student.name %></div>
    </li>
  <% end %>
</ul>

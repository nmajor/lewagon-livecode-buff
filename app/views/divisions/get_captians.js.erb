$("#division-group-list").html("<%= j render 'shared/group_list', groups: @groups %>");
$("#captains-modal").html("<%= j render 'captains', groups: @groups %>");
$("#captains-modal").modal({
  showClose: false,
});

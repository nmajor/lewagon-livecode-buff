<%= simple_form_for(@division) do |f| %>
  <%= f.error_notification %>

  <div class="form-inputs">
    <%= f.input :number_of_groups %>
  </div>

  <div class="form-actions">
    <%= f.button :submit %>
  </div>
<% end %>

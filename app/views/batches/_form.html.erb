<%= simple_form_for(@batch) do |f| %>
  <%= f.error_notification %>

  <div class="form-inputs">
    <%= f.input :num, label: "Number" %>
    <%= f.input :classmate_html, label: "Classmate Page HTML" %>
  </div>

  <div class="form-actions">
    <%= f.button :submit %>
  </div>
<% end %>
